# Requirements for Render deployment (without Selenium)
# User dashboard only - data fetching stays on Mac

flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0
python-dotenv==1.0.0
jinja2==3.1.2

# Optional: For database if you want to sync data
# psycopg2-binary==2.9.9  # PostgreSQL adapter
# sqlalchemy==2.0.23

